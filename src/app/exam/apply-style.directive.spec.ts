import { ApplyStyleDirective } from './apply-style.directive';

describe('ApplyStyleDirective', () => {
  it('should create an instance', () => {
    const directive = new ApplyStyleDirective();
    expect(directive).toBeTruthy();
  });
});
