import { CheckedPipePipe } from './checked-pipe.pipe';

describe('CheckedPipePipe', () => {
  it('create an instance', () => {
    const pipe = new CheckedPipePipe();
    expect(pipe).toBeTruthy();
  });
});
