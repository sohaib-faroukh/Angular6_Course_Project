import { FoucsedRowDirective } from './foucsed-row.directive';

describe('FoucsedRowDirective', () => {
  it('should create an instance', () => {
    const directive = new FoucsedRowDirective();
    expect(directive).toBeTruthy();
  });
});
